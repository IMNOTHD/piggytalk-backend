# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/apps/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Account
    version: 0.0.1
paths:
    /account/v1:
        post:
            operationId: Account_Login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginReply'
components:
    schemas:
        LoginReply:
            properties:
                token:
                    type: string
        LoginRequest:
            properties:
                account:
                    type: string
                    description: user account, like username, email, phone number
                password:
                    type: string
                    description: bcrypt(password+```salt```"piggytalk")
