# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/apps/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Test
    version: 0.0.1
paths:
    /snowflake:
        get:
            operationId: Test_TestSnowflake
            parameters:
                - name: dataCenterId
                  in: query
                  schema:
                    type: string
                - name: workerId
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TestSnowflakeReply'
components:
    schemas:
        TestSnowflakeReply:
            properties:
                snowFlakeId:
                    type: integer
                    format: int64
